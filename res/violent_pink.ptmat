Resource(shader, "demo") : shader;

Depth(operation = "ALWAYS");
Culling(mode = "NONE");
Polygon(mode = "LINE");
Priority(-1);

Shader(resource = @shader);

Uniform(binding = 2)
{
    [ 1.0, 0.0, 1.0, 1.0 ]     // colour
};