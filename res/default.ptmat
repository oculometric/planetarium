Resource(shader, "shr/demo") : shader;

Depth(operation = "LESS", test = 1, write = 1);
Culling(mode = "BACK");
Polygon(mode = "FILL");

Shader(resource = @shader);

Uniform(binding = 2)
{
    [ 0.2, 1.0, 0.2, 1.0 ],     // colour
    0.6                         // roughness
};